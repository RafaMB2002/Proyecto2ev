{# templates/login/index.html.twig #}
{% extends 'landing/landingPage.html.twig' %}

{% block title %}
    Inicio de sesion
{% endblock %}

{% block header %}
    <!-- Topbar Start -->
    <div class="container-fluid bg-primary text-white d-none d-lg-flex">
        <div class="container py-3">
            <div class="d-flex align-items-center">
                <a href="{{absolute_url(path('landing_page'))}}">
                    <h3 class="text-white fw-bold m-0">Xokas y Cía</h3>
                </a>
                <div class="ms-auto d-flex align-items-center">
                    <small class="ms-3"><i class="fa fa-map-marker-alt me-3"></i>C/ Ntra. Sra. de la Consolación nº8, Lahiguera, España</small>
                    <small class="ms-3"><i class="fa fa-envelope me-3"></i>rmonber2302@g.educaand.es</small>
                    <small class="ms-3"><i class="fa fa-phone-alt me-3"></i>+34 644 88 61 80</small>
                    <div class="ms-3 d-flex">
                        <a class="btn btn-sm-square btn-light text-primary rounded-circle ms-2" href="https://es-es.facebook.com/rafael.montoroberdonces.3/"><i
                                class="fab fa-facebook-f"></i></a>
                        <a class="btn btn-sm-square btn-light text-primary rounded-circle ms-2" href="https://www.instagram.com/rafaellh3_yt/"><i
                                class="fab fa-instagram"></i></a>
                        <a class="btn btn-sm-square btn-light text-primary rounded-circle ms-2" href="https://www.linkedin.com/in/rafael-montoro-berdonces-b80a66150"><i
                                class="fab fa-linkedin-in"></i></a>
                    </div>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <small class="ms-4">{{app.user.email}}</small>
                        {% endif %}
                </div>
            </div>
        </div>
    </div>
    <!-- Topbar End -->
{% endblock %}


{% block body %}
    <div class="container">
        {# <div class="row justify-content-center p-2">
            {% if error %}
            <div>Correo o contraseña incorrecta{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
        </div> #}
        <div class="card">
            <div class="card-header text-center">
                <h3>Inicio de sesion</h3>
            </div>
            <div class="card-body text-center">
                <form action="{{ path('app_login') }}" method="post">
            <div class="row justify-content-center p-2">
                <label for="username" class="col-6">Email:</label>
                <input type="text" id="username" name="_username" value="{{ last_username }}" class="col-6">
            </div>
            <div class="row justify-content-center p-2">
                <label for="password" class="col-6">Contraseña:</label>
                <input type="password" id="password" name="_password" class="col-6">
            </div>
            

            {# If you want to control the URL the user is redirected to on success #}
            
            {#  <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"> #}
            
            <div class="row justify-content-center p-2">
                <input type="hidden" name="_target_path" value="/">
                <div class="col-md-12">
                    <button type="submit" class="btn btn-primary rounded-pill py-2 px-3">logearse</button>
                </div>
                {% if error %}
            <div class="alert alert-info">Correo o contraseña incorrecta{# {{ error.messageKey|trans(error.messageData, 'security') }} #}</div>
        {% endif %}
                
            </div>
            
        </form>
            </div>
        </div>
        
    </div>
{% endblock %}

{% block footer %}
    
{% endblock %}
